{
  "name": "@p2flux/sdk-php",
  "version": "0.1.0",
  "private": false,
  "description": "PHP SDK for P2Flux. This npm manifest exists only so P2Flux/core can pin this repository by tag and run the integration suite against it; composer.json is the real manifest for PHP consumers.",
  "license": "MIT",
  "repository": { "type": "git", "url": "git+https://github.com/P2Flux/sdk-php.git" },
  "files": ["src", "tests", "composer.json"]
}
